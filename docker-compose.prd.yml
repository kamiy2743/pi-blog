services:
  nginx:
    build:
      context: ./
      dockerfile: ./nginx/Dockerfile.prd
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /run/:rw,uid=101,gid=101,mode=0755
      - /var/cache/nginx/:rw,uid=101,gid=101,mode=0755
    networks:
      - private
    depends_on:
      - go
      - ssr
    restart: unless-stopped

  cloudflared:
    image: cloudflare/cloudflared:2026.2.0
    volumes:
      - ./cloudflared/config.yml:/etc/cloudflared/config.yml:ro
    command: tunnel --no-autoupdate --config /etc/cloudflared/config.yml run --token-file /run/secrets/cloudflared_tunnel_token
    secrets:
      - cloudflared_tunnel_token
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    networks:
      - private
      - egress
    depends_on:
      - nginx
    restart: unless-stopped

  go:
    build:
      context: ./backend/
      dockerfile: ./Dockerfile.app
    env_file: ./backend/.env.prd
    secrets:
      - mysql_user
      - mysql_user_password
      - admin_basic_auth_user
      - admin_basic_auth_pass
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    networks:
      - private
    depends_on:
      - mysql
    restart: unless-stopped

  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD_FILE: /run/secrets/mysql_root_password
      MYSQL_USER_FILE: /run/secrets/mysql_user
      MYSQL_PASSWORD_FILE: /run/secrets/mysql_user_password
      MYSQL_DATABASE: blog
    command: ["mysqld", "--character-set-server=utf8mb4", "--collation-server=utf8mb4_unicode_ci"]
    secrets:
      - mysql_root_password
      - mysql_user
      - mysql_user_password
    security_opt:
      - no-new-privileges:true
    volumes:
      - mysql_data:/var/lib/mysql/
    networks:
      - private
    restart: unless-stopped

  ssr:
    build:
      context: ./frontend/
      dockerfile: ./Dockerfile.ssr
    env_file: ./frontend/.env
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    networks:
      - private
    restart: unless-stopped

networks:
  private:
    internal: true
  egress:

volumes:
  mysql_data:

secrets:
  cloudflared_tunnel_token:
    file: ./secrets/prd/cloudflared_tunnel_token
  admin_basic_auth_user:
    file: ./secrets/prd/admin_basic_auth_user
  admin_basic_auth_pass:
    file: ./secrets/prd/admin_basic_auth_pass
  mysql_root_password:
    file: ./secrets/prd/mysql_root_password
  mysql_user:
    file: ./secrets/prd/mysql_user
  mysql_user_password:
    file: ./secrets/prd/mysql_user_password
